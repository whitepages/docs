---
title: Get an Event
description: Retrieve a specific property deed event by its unique identifier
---

Use the `GET /v1/events/{event_id}` endpoint to retrieve a single event by its ID. This is useful for fetching the full details of an event referenced in a webhook delivery or for looking up a specific transaction.

<Callout type="warn" title="Invite-Only Access">
  The Events API is currently available by invite only. To request access,
  contact our support team at
  [support@whitepages.com](mailto:support@whitepages.com) or visit the [Support
  and Incident Response](/references/support-and-incident-response) page.
</Callout>

## Basic Usage

Retrieve an event by its ID:

```bash title="Request"
curl 'https://api.whitepages.com/v1/events/evt_abc123' \
  --header 'X-Api-Key: YOUR_API_KEY'
```

## Path Parameters

| Parameter  | Type   | Required | Description                                             |
| ---------- | ------ | -------- | ------------------------------------------------------- |
| `event_id` | string | Yes      | The unique identifier of the event (e.g., `evt_abc123`) |

## Response

The response returns the full event object:

```json title="Response"
{
  "id": "evt_abc123",
  "object": "event",
  "event_type": "deed.transfer",
  "event_date": "2026-02-27T00:00:00Z",
  "created_at": "2026-02-27T12:00:00Z",
  "jurisdiction": {
    "county": "King",
    "state": "WA",
    "fips_code": "53033"
  },
  "property": {
    "id": "prop_123",
    "address": {
      "line1": "123 Main St",
      "city": "Seattle",
      "state": "WA",
      "zip": "98101",
      "county": "King"
    },
    "apn": "123456-7890",
    "location": {
      "latitude": 47.6062,
      "longitude": -122.3321
    }
  },
  "parties": [
    {
      "role": "grantor",
      "name": "Jane Doe",
      "type": "individual"
    },
    {
      "role": "grantee",
      "name": "John Smith",
      "type": "individual"
    }
  ],
  "detail": {
    "document_number": "20260227001234",
    "recorded_date": "2026-02-27",
    "consideration": 750000,
    "transfer_reason": "sale"
  },
  "source": {
    "name": "king_county_recorder"
  }
}
```

<Callout type="tip" title="Webhook Integration">
  When your webhook endpoint receives an event delivery, the payload includes
  the full event object. Use this endpoint to re-fetch an event if you need to
  reprocess it or verify its contents.
</Callout>

For full request and response schemas, see the [Retrieve a single event by ID](/references/events/get_event) API reference.
